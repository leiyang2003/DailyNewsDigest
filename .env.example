# OpenAI API Key - 在 https://platform.openai.com/api-keys 创建
OPENAI_API_KEY=your_openai_api_key_here

# 可选：「当日」采用的时区（IANA），默认北京时间，避免中美时差歧义
# DIGEST_TIMEZONE=Asia/Shanghai

# 可选：单次请求超时秒数，默认 3600（1 小时）。web_search 较久时可改为 7200
# DIGEST_TIMEOUT=3600

# 可选：代理（网络受限时使用），例如 http://127.0.0.1:7890
# HTTP_PROXY=
# HTTPS_PROXY=

# 可选：TTS 播客音频（tts.py）。需先安装 ffmpeg（pydub 依赖）
# TTS_MODEL=tts-1-hd
# TTS_VOICE=nova
# TTS_SPEED=1.0

# Google 登录（温故知新等需登录功能）：在 Google Cloud Console 创建 OAuth 2.0 客户端（Web 应用），配置授权重定向 URI
# GOOGLE_CLIENT_ID=your_google_client_id
# GOOGLE_CLIENT_SECRET=your_google_client_secret
# GOOGLE_REDIRECT_URI=http://localhost:5003/auth/callback
# SECRET_KEY=随机长字符串（生产环境必改）

# Vercel Cron：每天 1:00（北京时间）调用 /api/cron/run_daily 时必填
# CRON_SECRET=随机长字符串（至少 16 位，Vercel 会以 Authorization: Bearer <CRON_SECRET> 调用）
# CRON_USER_ID=要执行流水线的用户 ID（与 Google 登录后的 user_id 一致，见 reports/user_settings_<id>.json 等）
# CRON_RUN_TIMEOUT=300   # 可选，流水线超时秒数，默认 300；Vercel Pro 可设更大
